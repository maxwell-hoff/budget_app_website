services:
  - type: web
    name: workbench-budgeting
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --bind 0.0.0.0:$PORT serve:app
    envVars:
      - key: PYTHON_VERSION
        value: "3.12.0"
